# Paper

Main source:

- `zk-otp.tex`

Compile with:

```bash
pdflatex zk-otp.tex
pdflatex zk-otp.tex
```

The paper is written as a single-file LaTeX draft for IACR ePrint or a
systems-security workshop. It uses standard packages only.
